<div class='booyah-box col-xs-10 col-xs-offset-1'>
	<h1>Enter Tea Place</h1>
	<br />
		<%= simple_form_for @place do |f| %>
			<%= f.input :name %>
			<%= f.input :address %>
			<%= f.input :description %>
			<br/>
			<%= f.submit 'Create', :class => 'btn btn-primary' %>
		<% end %>
</div>